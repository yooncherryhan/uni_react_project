export const CreateTypeButton = ({ type }) => {
    return (
        <div className='bg-blue-600 p-2 w-[70px] text-center rounded-lg text-[#fff]'>
            <button type={type}>Create</button>
        </div>

    );
};
